$(".followings-count-" + <%= current_user.id.to_s %>).html("<%= j(render 'relationships/followings-counter', user: current_user) %>");
$(".followers-count-" + <%= @user.id.to_s %>).html("<%= j(render 'relationships/followers-counter', user: @user) %>");
$(".relationship-btn-" + <%= @user.id.to_s %>).html("<%= j(render 'relationships/follow-btn', user: @user) %>");